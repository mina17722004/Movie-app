import 'package:flutter/material.dart';

class ExploreWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Exploree Screen",
          style: TextStyle(color: Colors.white, fontSize: 24)),
    );
  }
}